// Define the catalog page metadata interface
export interface CatalogsPageMetadata {
  url: string;
  baseUrl: string; // Base URL of the site
  title: string;
  description: string;
  imageUrl: string;
  pageUrl: string;
}
