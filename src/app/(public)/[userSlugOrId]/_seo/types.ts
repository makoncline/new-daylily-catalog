// Define the profile metadata interface without importing the PublicProfile type
export interface ProfileMetadata {
  url: string;
  title: string;
  description: string;
  imageUrl: string;
  pageUrl: string;
}
