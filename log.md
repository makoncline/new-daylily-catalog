- [2026-07-22] Next route types after branch reset: `next typegen` refreshes `.next/types` but can leave removed routes in `.next/dev/types`, which then makes `tsc --noEmit` compare incompatible route unions. When typecheck names routes absent from the current branch after a reset, move or remove the generated `.next/dev` directory before rerunning typecheck.
- [2026-07-22] Cultivar listing link surfaces: Indexable cultivar pages link to seller listings from offer rows, garden photos, and Offer JSON-LD. When changing canonical listing discovery, update and scan all three surfaces; keep legacy `?viewing=` URLs only for noindex search and deep-link behavior.
- [2026-07-23] Search Console sitemap cohorts: The submitted sitemap index reports discovered counts for each child sitemap, but page-indexing drilldowns can stay disabled until Google refreshes its coverage report. Use a dedicated overlapping child sitemap to measure a high-value URL cohort without removing those URLs from broader discovery shards.
- [2026-07-23] Database-backed sitemap builds: A new ISR sitemap route can make concurrent sitemap prerenders exceed Next's 60-second static-generation limit even when the query is fast against the local replica. Keep database-backed sitemap indexes and new child routes dynamic, preserve explicit CDN cache headers, and verify the production build path.
- [2026-07-23] Public listing sitemap ownership: Keep listing detail URLs in paginated listing child sitemaps instead of the mixed main sitemap. Select `userId` with each listing and resolve the small seller-slug set once; nested profile selection makes Prisma chunk thousands of listing-owner lookups. This gives Search Console a distinct listing cohort, removes the unbounded listing query from static main-sitemap generation, and keeps seller profiles and their pagination in the main sitemap.
- [2026-07-26] Enhanced importer column boundary: Parsed XLSX rows can retain long trailing empty cell arrays, so maximum row length can place added columns far to the right. When adding enhanced-original columns, compute the boundary from the last nonblank source value and preserve existing identity columns.
- [2026-07-26] Split data-table vertical scrolling: The pinned and center sections in the shared DataTable must not use independent vertical scroll containers with scroll-event synchronization; browser scroll events can keep bouncing and saturate a renderer. When a table uses pinned columns, keep the proven page-scroll behavior used by dashboard listings or design one shared vertical scroll owner.
- [2026-07-27] Importer checkout integration metadata: The integration Stripe provider must return the `entry_source` and `return_to` session metadata that checkout creation sends. When the fixture omits these fields, the success page cannot recognize the importer session and blocks the importer-only handoff even though production code is correct.
- [2026-07-27] Hydration readiness in full-app tests: React private fields such as `__reactProps$` are not stable readiness signals. When a controlled form must not accept input before hydration, use the existing `useIsHydrated` hook to disable the control and have Playwright wait for the control to become enabled.
- [2026-07-27] Auth-aware importer checkout route ownership: Public-surface cache tests treat every source file under `src/app/(public)` as auth-free, including nested checkout routes. When `/catalog-importer/checkout` needs Clerk and tRPC providers, keep its physical route under `src/app/catalog-importer/checkout` with its own `AuthProviders` and `PublicShell`; the public importer page can remain under the auth-free route group at the same URL prefix.
- [2026-07-27] Clerk E2E retry cleanup: Deleting a Clerk test user does not remove its local application rows because a localhost test run does not receive the Clerk deletion webhook. When an E2E creates a Clerk user, remove the local user and related checkout/cache keys in `finally`, and use a unique public slug so CI retries cannot collide.
- [2026-07-29] Importer checkout skips profile setup: A successful importer checkout now claims the subscription and returns directly to `/dashboard/imports`; name, location, and public URL remain optional profile settings. When changing importer onboarding, do not add a profile gate between checkout and the preserved browser-local import.
- [2026-07-29] PostHog importer correlation: `data-ph-capture-attribute-*` suffixes become event property names verbatim, and `posthog.reset()` must not run for a truly anonymous checkout return. Use snake_case suffixes such as `import_id`, and reset only when `$user_id` is present.
- [2026-07-29] Cloudflare cache-tag rollout: `Cache-Tag` headers do not tag objects that Cloudflare already cached. When a release introduces tag-based deploy purges, purge the hostname once after the tagged release reaches origin; later deploys can purge only the stable tag.
- [2026-07-30] Ahrefs representative crawl scope: A custom-list-only crawl with maximum depth 0 reports sitemap and orphan warnings because sitemap sources and link traversal are disabled. Use this crawl for route-template checks, and validate sitemap membership separately.
- [2026-07-30] Prod-like crawl analytics isolation: `prepare-prod-like-local-smoke.mjs` disables Sentry but retains the production PostHog configuration. Before a crawler uses `dev.daylilycatalog.com`, set `NEXT_PUBLIC_POSTHOG_KEY: ""` in the ignored prod-like Compose override and verify `/api/runtime-config` reports PostHog disabled.
- [2026-07-31] Pure metadata test imports: Importing a database-backed App Router module can trigger environment validation before Vitest executes a test, while local environment files can hide the problem. Keep pure metadata builders in database-free modules and import those modules directly in focused tests.
- [2026-07-31] libSQL embedded-replica close boundary: `@libsql/client` 0.17.x uses native `libsql` 0.5.29 with SQLite 3.45.1, and an open upstream issue shows that `close()` can retain prepared statements and WAL locks until garbage collection. A close call does not make a managed replica file safe for stock SQLite. Keep replica access in libSQL.
- [2026-08-05] Next 16.3 Sharp alignment: Next 16.3.0 resolves Sharp 0.35.3. When upgrading Next.js, keep the app's direct Sharp pin aligned and verify `pnpm why sharp` shows one version so Satori and ImageAsset processing do not load two libvips runtimes.
- [2026-08-05] Next 16.3 Turbopack runtime paths: Next 16.3 warns when a dynamic file path makes Turbopack trace the full project. For runtime-only paths such as feature-flag input and local profiler output, add the documented `turbopackIgnore` annotation at the path operation and confirm the production build has no full-project trace warning.
- [2026-08-05] Next 16.3 standalone package boundary: Next 16.3.0 includes `.next/package.json` in its required server files. When upgrading from 16.2.6, remove the local Next patch and its Docker `COPY patches` step instead of retargeting it, and confirm the standalone build contains one `.next/package.json` manifest entry.
- [2026-08-05] Next 16.3 Vercel adapter boundary: Next 16.3.0 cannot combine `output: "standalone"` with Vercel's injected build adapter because the adapter omits the whole-server NFT that standalone finalization reads. Keep standalone output for Docker, disable it when `VERCEL=1`, include `VERCEL` in Turbo's build cache hash, and verify both build paths after Next upgrades.
- [2026-08-06] Chrome extension screenshot persistence: A Chrome screenshot call can return valid image bytes without writing its requested `path`. When final browser evidence must remain after the controlled tab closes, write the returned bytes to the ignored evidence directory and verify the file exists before finalizing the browser task.
- [2026-08-07] Next 16.3 OG Sharp loader collision: Next image optimization blocks Sharp SVG loading for security, but the Next 16.3 Node `ImageResponse` renderer then cannot convert Satori SVG through the same Sharp instance. This matters for self-hosted dynamic Open Graph routes after any optimized-image request. Keep the optimizer restriction, use the bundled Resvg fallback for OG rendering, and reproduce the condition by calling `getSharp()` before consuming an `ImageResponse` body.
- [2026-08-24] Rebloom search field: AHS V2 stores rebloom as a dedicated flag, not as a bloom-season label. Keep Bloom Season limited to the initial bloom period and expose rebloom as an independent Boolean filter so customers can combine both traits.
- [2026-08-29] libSQL replica search source: A libSQL-managed embedded replica file is not portable SQLite. Search builds must explicitly sync the normal replica, page rows only through the singleton `replicaDb`, and stream bounded pages to a target-only child that never opens the replica. Never open or copy the replica file with stock SQLite.
- [2026-08-29] Search refresh concurrency boundary: The VPS runs one Compose app container with one standalone Node process, so the shared `globalThis` refresh promise is the complete refresh lock. Do not add a persistent file lock unless the deployment adds multiple app processes that share `/data`; a stale file lock delays crash recovery without coordinating Vercel isolates.
- [2026-08-29] Reusable replica producer seam: `syncEmbeddedReplica()` returns the exact synced singleton and fails closed in production, while `streamToTargetWorker()` owns bounded NDJSON transport to a target-only child. One-shot artifact producers should reuse those two exports, keep their keyset queries and target validation private, and never pass a replica path or client to the child.
- [2026-08-29] Target worker bounded input: `readline.Interface` async iteration can queue many complete NDJSON pages while a slow target write is active. Target-only artifact workers must pull raw stdin chunks on demand and parse lines incrementally so producer backpressure also bounds child memory.
- [2026-08-29] Shared edge network service names: Docker Compose projects that join the same external network can expose the same service alias to Caddy. When two app stacks share the `edge` network, use a unique service name for each upstream and do not name both services `app`.
- [2026-08-29] Vercel ignored-build exit boundary: The ignored-build command runs from the project Root Directory. Exit code 0 cancels the build and exit code 1 builds. Use a repository-relative command and build when comparison data is unavailable.
- [2026-08-29] Deployment-status classifier trust boundary: A deployment SHA can contain untrusted repository code while a workflow has later write-capable jobs. Check out the default branch separately, run its classifier against the candidate worktree with persisted credentials disabled, and keep issue or pull-request writes in a later job that never checks out candidate code.
- [2026-08-29] Trusted classifier tool resolution: Running a trusted script from an untrusted worktree can still execute candidate `node_modules/.bin` tools or use its package-manager configuration. Install the fixed tool from the trusted checkout and pass its absolute path to the classifier.
- [2026-08-29] Preview secret provenance: A trusted deployment-status creator does not prove that the deployed SHA belongs to the repository. Before candidate tests receive preview secrets, require the SHA to equal the head of a fetched origin branch, or run only trusted test code. Treat a missing or mismatched origin ref as untrusted.
- [2026-08-29] Large seller storefront snapshots: A nested Prisma seller query can exceed SQLite's parameter limit above 3,000 listings, and paged Prisma mapping can still retain about 200 MiB during a cold snapshot build. For large public seller documents, map bounded batches in a short-lived builder, publish a content-addressed artifact with an atomic manifest, and keep the request route file-only.
