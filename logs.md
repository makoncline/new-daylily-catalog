- [2026-09-22] Docker adapter smoke path: Turbopack packages the local Prisma SQLite adapter under `.pnpm` without a top-level module link. A direct require from `server.js` can fail while `/catalogs` works. For final-image tests, verify the local adapter through the compiled app with a disposable database; use the server resolver for the explicit production libSQL and Sharp dependencies.
- [2026-09-23] Search cleanup proof: The retained local reader-proof TLS certificates expire. Renew the local CA and server certificate before reusing the fixture; do not disable TLS verification. Stop the old app before starting a replacement on the same replica volume. The cleanup keeps the candidate artifact and timer names; rollback uses the prior reader image with its candidate flag true and legacy refresh interval zero.
- [2026-09-24] Cultivar OG rendering: Next 16 ImageResponse rejects WOFF2 fonts and Satori ignored the `inset` shorthand for absolute overlays. When editing server-rendered social cards, use TTF or OTF fonts, explicit top/left/width/height values, and inspect a generated PNG.
- [2026-09-24] Local social-card preview refresh: The port 3214 Next dev process kept serving the same PNG bytes for new query versions after source edits. When a share-card preview does not reflect changed source, compare hashes and restart that worktree's dev server before judging the layout.
- [2026-09-24] ImageResponse logo legibility: A full-width footer gradient obscured cultivar photos in the print card. Satori can render layered white text shadows and a duplicated white SVG stroke behind a colored logo icon when glyph-local treatment is needed.
- [2026-09-24] Cultivar print logo decision: The print variant uses the same localized 410px dark footer fade, cream wordmark, and amber icon as the share variant. When changing logo styling, keep both variants identical and avoid a full-width footer fade.
- [2026-09-24] Cultivar print set: Flossy May Two has no cultivar database record, so the one-off print page was rendered directly through `CultivarSocialCard` from the Rolling Oaks Daylilies source photo and seeding details. When rebuilding the 14-page PDF, fetch the other 13 print images from the single-cultivar OG route and render Flossy separately; keep one 1200×630 image per PDF page.
- [2026-09-24] Cultivar long-detail wrapping: The long-detail outer flex width and text max width both needed a 480px cap so color and parentage wrap before the photo fade at x=552. When changing social-card text width, update both bounds, inspect the longest color field on Viva Glam Girl for bottom fit, and bump the versioned cultivar OG URL to bypass Cloudflare's prior image.
- [2026-09-24] Satori gradient syntax: Next ImageResponse failed with `object null is not iterable` when a `linear-gradient` value used newline-separated color stops, but rendered the same eased multi-stop gradient after joining the stops into one line. When adjusting OG fades, keep the CSS gradient value on one line and verify a real PNG render.
